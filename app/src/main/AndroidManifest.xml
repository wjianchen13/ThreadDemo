<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThreadDemo"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pool.ThreadPoolActivity" />
        <activity android:name=".pool.ThreadPoolActivity1" />
        <activity android:name=".pool.ThreadPoolActivity2" />
        <activity android:name=".pool.ThreadPoolActivity3" />
        <activity android:name=".pool.ThreadPoolActivity4" />
        <activity android:name=".test2.TestActivity2" />
        <activity android:name=".test4.TestActivity4" />
        <activity android:name=".test4.TestActivity41" />
        <activity android:name=".test4.TestActivity42" />

    </application>

</manifest>